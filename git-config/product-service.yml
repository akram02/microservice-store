server:
  port: 8600
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/product
    username: root
    password: akram

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect


catalog:
  sample:
    data: Hello Worldss !!
